// const data = require('../data/zoo_data');

// const { species } = data;
// const NE = species.filter(({ location }) => location === 'NE').map((info) => info.name);
// const NW = species.filter(({ location }) => location === 'NW').map((info) => info.name);
// const SE = species.filter(({ location }) => location === 'SE').map((info) => info.name);
// const SW = species.filter(({ location }) => location === 'SW').map((info) => info.name);

// // Function to return the species by location if no parameters is assigned
// function getAnimalsByLocation() {
//   return { NE, NW, SE, SW };
// }

// // Function to include names of each animal
// function getAnimalNames(animal) {
//   return {
//     [animal]: species.find((specie) => specie.name === animal).residents.map((inf) => inf.name),
//   }
// }

// // Main function to call other functions above acording to each condition
// // function getAnimalMap({ includeNames = false, sorted = false, sex = '' } = {}) {
// // }

// // module.exports = getAnimalMap;
