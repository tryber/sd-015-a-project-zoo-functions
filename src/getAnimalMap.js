// ref: https://github.com/tryber/sd-015-a-project-zoo-functions/pull/127
const data = require('../data/zoo_data');

function getAnimalMap(options) {
  // seu código aqui
}
console.log(getAnimalMap({ includeNames: true }));
module.exports = getAnimalMap;
