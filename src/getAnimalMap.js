const data = require('../data/zoo_data');

// function getSpecieByRegion(region) {
//   return (data.species.filter((specie) =>
//     (specie.location === region))).map((animal) => animal.name);
// }

// function getAnimalsByRegion() {
//   const speciesRegions = { NE: [], NW: [], SE: [], SW: [] };
//   speciesRegions.NE = getSpecieByRegion('NE');
//   speciesRegions.NW = getSpecieByRegion('NW');
//   speciesRegions.SE = getSpecieByRegion('SE');
//   speciesRegions.SW = getSpecieByRegion('SW');
//   return speciesRegions;
// }

// function getAnimalsWithOptions(option) {
//   const species = getAnimalsByRegion();
// }
function getAnimalMap(options) {
  // seu código aqui

}

module.exports = getAnimalMap;
