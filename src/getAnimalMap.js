const data = require('../data/zoo_data');
// const { species } = require('../data/zoo_data');

// const allLocations = { NE: 0, NW: 0, SE: 0, SW: 0 };
// let { NE, NW, SE, SW } = allLocations;

// const AnimalPerRegion = () => {
//   NE = species.filter((animal) => animal.location === 'NE').map((animal) => animal.name);
//   NW = species.filter((animal) => animal.location === 'NW').map((animal) => animal.name);
//   SE = species.filter((animal) => animal.location === 'SE').map((animal) => animal.name);
//   SW = species.filter((animal) => animal.location === 'SW').map((animal) => animal.name);
//   return { NE, NW, SE, SW };
// };

function getAnimalMap() {
//   result = [];
//   if (!options) {
//     return AnimalPerRegion();
//   } if (options.includes(includesNames = true)) {
//     return 'x';
//   }
}
// console.log(getAnimalMap({ includesNames: true }));

module.exports = getAnimalMap;
