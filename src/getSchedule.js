// const { hours } = require('../data/zoo_data');
const data = require('../data/zoo_data');

function getSchedule(scheduleTarget) {
  /* const daySem = Object.keys(hours);
  const horario = Object.values(hours);
  console.log(daySem);
  const animaisTuesday = data.species.filter((animal) => animal.availability.includes('Tuesday'));
  const animTuesday = animaisTuesday.map((bicho) => bicho.name);
  console.log(animTuesday);
  console.log(horario);
  const { open, close } = horario;
  console.log(hours.values);
  // const modelo = {};

  return animaisTuesday;  */
}
// getSchedule();
module.exports = getSchedule;
/*  'Tuesday': {
        'officeHour': 'Open from 8am until 6pm',
        'exhibition': [ 'lions', 'tigers', 'bears', 'penguins', 'elephants', 'giraffes' ],
      } */
