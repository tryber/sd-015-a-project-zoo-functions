const data = require('../data/zoo_data');

function countAnimals(animal) {
  let objAnimal = {};
  if (!animal){
    
  }
  // const animals = data.species.filter((animalz) => animal.name === animalz.residents;
  // console.log(animals);
  // seu código aqui
};
console.log(countAnimals();
module.exports = countAnimals;
