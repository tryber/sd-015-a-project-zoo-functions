const data = require('../data/zoo_data');

function countAnimals(animal) {
  if (animal === {}) {
    console.log(data.species.map((element) => `${element.name} abcabctodacriança`));
  }
}

countAnimals({});

module.exports = countAnimals;
