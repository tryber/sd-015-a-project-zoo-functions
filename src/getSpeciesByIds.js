const data = require('../data/zoo_data');
const { species } = require('../data/zoo_data');

function getSpeciesByIds(...ids) {
  // if (ids === undefined) {
  //   return [];
  // }
  // return species.filter((animals, index) => animals.id === ids[index]);
}

module.exports = getSpeciesByIds;
