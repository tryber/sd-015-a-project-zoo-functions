const data = require('../data/zoo_data');

/*
function getSpecieLocation(whatSpecie) {
  return data.species.find((specie) => specie.id === whatSpecie).location;
}

function employeesList() {
  let newObj = {};
  const resultArray = [];

  data.employees.forEach((employee) => {
    newObj = { id: employee.id, fullName: `${employee.firstName} ${employee.lastName}`, species: employee.responsibleFor };
    resultArray.push(newObj);
    newObj = {};
  });
  return resultArray;
}

console.log(employeesList());

function getEmployeesCoverage(inputObj) {
  // Escreva sua função aqui
}

module.exports = getEmployeesCoverage;
*/
